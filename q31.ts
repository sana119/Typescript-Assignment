// exercise 31

let ueserName: string[] = ['hira','hina','mehak','danish','daniyal'];
if(ueserName.length === 0){
    console.log('We need to find some users!')
}else{
    ueserName = [];
    console.log('All usernames have been removed. ' + ueserName.length);
}
