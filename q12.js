"use strict";
// Exercise 12
let members = ['maha', 'habiba', 'nimrah', 'saba', 'safia'];
let message = 'hi how are you? what are you doing now a days:';
for (let i = 0; i < members.length; i++) {
    console.log(message + members[i]);
}
